#pragma once
#include <iostream>
#include <vector>
#include "serialize.h"
#include <fstream>

// Функция для получения настроек из файла
// Возвращает: вектор пар "ключ-значение" с настройками игры
// Может выбрасывать исключения при ошибках чтения файла
std::vector<std::pair<std::string, std::string>> getSettings();

// Функция для сохранения настроек в файл
// Параметр data: вектор пар "ключ-значение" с настройками для сохранения
// Не возвращает значения (void)
void setSettings(std::vector<std::pair<std::string, std::string>> data);

// Функция для установки настроек по умолчанию
// Создает файл с настройками по умолчанию, если он не существует
// Или сбрасывает настройки к значениям по умолчанию
// Не возвращает значения (void)
void setDefaultSettings();